{
  "name": "@nestify/backend",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "lint": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@nestify/shared": "workspace:*",
    "hono": "^4",
    "drizzle-orm": "^0.38",
    "postgres": "^3",
    "@hono/zod-validator": "^0.4",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.9",
    "drizzle-kit": "^0.30",
    "typescript": "^5"
  }
}
