{
  "name": "nestify-root",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9",
    "turbo": "latest"
  }
}
